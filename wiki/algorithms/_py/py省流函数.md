### 序
py有太多太多省流方式，欸，别说，您瞧怎么着，反倒是省流版因为大量使用了底层c库开销更小。
我一般把这些称为省流函数，因为他们远比自己实现开销更小（自己实现的方式大多在py层，所以更慢）。然而，这种省流函数种类众多，形式各不相同，若按类别总结往往会有很多极为罕见的省流函数出现。因此，下文我不会对省流函数进行细致分类，就着看吧。

### 省流函数
- a
```python
s=''.join(format(i, 'b') for i in range(1,n+1))
```
